# GitHub Setup — CodeAlpha Social Media Platform

Repository:

`https://github.com/FaisalRayyan/CodeAlpha_SocialMediaPlatform`

For the final project folder:

```powershell
git remote set-url origin https://github.com/FaisalRayyan/CodeAlpha_SocialMediaPlatform.git
git branch -M main
git status
git add .
git commit -m "feat: complete SocialSphere full-stack platform"
git push origin main
```

Verify:

```powershell
git remote -v
git status
git log --oneline -5
```

Expected final status after push:

```text
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
```
